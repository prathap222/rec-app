import React from "react";
import ChoicesListItems from "./ChoicesListItems";

export default function ChoicesList() {
  // //the array of DayList's child DayListItem component
  // let dayListItemArr = props.days.map((d) => {
  //   return (
  //     <ChoicesListItems
  //       key={d.id}
  //       name={d.name}
  //       spots={d.spots}
  //       selected={d.name === props.value}
  //       setDay={props.onChange}
  //     />
  //   );
  // });

  return (
    <ul>
      {/* {dayListItemArr} */}
    </ul>

  );
}
